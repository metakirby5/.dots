#!/usr/bin/env bash

killall lemonbar 2>/dev/null
emit-lemonblocks \
  -B "{{ bgc }}" \
  -F "{{ fgc }}" \
  -U "{{ fgc }}" \
  --urgent "{{ b_red }}" \
  2>/dev/null |\
  lemonbar -u {{ bar_underline }} -f "{{ bar_font }}" |\
  sh &>/dev/null &

