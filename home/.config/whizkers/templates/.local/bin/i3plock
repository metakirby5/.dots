#!/usr/bin/env bash
# Pixellated lockscreen.
# Requires i3lock-color.

TMPBG="$(mktemp).png"

scrot "$TMPBG"
mogrify \
	-sample 5% -sample 2000% \
	-fill black -colorize 50% \
	-font "{{ font }}-Regular" -kerning 5 -pointsize 100 \
	-fill white -gravity center -annotate +0+180 "locked" \
	"$TMPBG"
killall -SIGUSR1 dunst # pause dunst
i3lock -ndi "$TMPBG" \
	--ringvercolor={{ n_black_nohash}}ff \
	--insidevercolor={{ n_black_nohash }}ff \
	--ringwrongcolor={{ n_red_nohash}}ff \
	--insidewrongcolor={{ n_red_nohash}}ff \
	--ringcolor={{ bgc_nohash }}ff \
	--insidecolor={{ bgc_nohash }}ff \
	--linecolor=00000000 \
	--textcolor=00000000 \
	--keyhlcolor={{ n_primary_nohash }}ff \
	--bshlcolor={{ n_secondary_nohash }}ff
killall -SIGUSR2 dunst # resume dunst
rm "$TMPBG"

