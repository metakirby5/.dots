# Me!
realname: "Ethan C"
username: "metakirby5"

# System
xft_dpi: 276

# Fonts
font: "PixelMPlus12"
term_fonts:
  - "PixelMPlus12"
  - "M+ 1p"
  - "Baekmuk Gulim"
use_bold: false
pixelsize: 28
charheight: 31
charwidth: 15
term_letterspace: 2

# WM
wallpapers: "/usr/share/backgrounds/in_rotation/"
floating: false
inner_gap_size: 15
dunst_gap: 20
border_size: 5
dmenu_flags: '-i -f -fn "{{ font }}:pixelsize={{ pixelsize }}" -nf "{` {{ colors }}["foreground"] `}" -nb "{` {{ colors }}["background"] `}" -sf "{` {{ colors }}["foreground"] `}" -sb "{` {{ colors }}[{{ colors }}["primary"]]["normal"] `}" -h {{ bar_height }} {{ #bar_on_bottom }} -b {{ /bar_on_bottom }}'
dunst_dmenu_flags: '-i -f -fn "{{ font }}:pixelsize={{ pixelsize }}" -nf {` {{ colors }}["foreground"] `} -nb {` {{ colors }}["background"] `} -sf {` {{ colors }}["foreground"] `} -sb {` {{ colors }}[{{ colors }}["primary"]]["normal"] `} -h {{ bar_height }} {{ #bar_on_bottom }} -b {{ /bar_on_bottom }}'
dunst_geometry: "0x5-{` {{ inner_gap_size }} + {{ dunst_gap }} `}+{` {{ bar_height }} + {{ inner_gap_size }} + {{ dunst_gap }} `}"

# Compton
shadows: false

# Bar
bar_height: 50
bar_on_bottom: false
bar_conky_height: "{` {{ bar_height }} + {{ inner_gap_size }} `}"
bar_black_fgc: false
bar_white_fgc: false
bar_bgc: "#00{` {{ colors }}['background'].lstrip('#')  `}"
bar_block_bgc: "#ff{` {{ colors }}['background'].lstrip('#')  `}"
bar_underline_color: "#88{` {{ colors }}['foreground'].lstrip('#') `}"
bar_fonts:
  - "PixelMPlus12-7"
  - "M+ 1p-7"
  - "Baekmuk Gulim-7"
bar_icons: "-wuncon-siji-medium-r-normal--*-225-*-*-c-*-iso10646-1"
bar_font_offset: -5
bar_underline: 0

# GTK
gtk_theme: "oomox-whizkers"
gtk_icon_theme: "Numix-Circle"
gtk_cursor_theme: "Adwaita"

# Colors

colors:
  name:         "untitled"
  primary:      "blue"
  secondary:    "red"
  background:   "#272727"
  foreground:   "#fafafa"
  black:
    normal:     "#353540"
    bold:       "#545466"
  red:
    normal:     "#aa706b"
    bold:       "#d28a84"
  green:
    normal:     "#aaaa76"
    bold:       "#d2d291"
  yellow:
    normal:     "#c8b8a1"
    bold:       "#f0dcc1"
  blue:
    normal:     "#87aaa6"
    bold:       "#a6d2cd"
  magenta:
    normal:     "#7cb6c8"
    bold:       "#94daf0"
  cyan:
    normal:     "#82aa88"
    bold:       "#a0d2a8"
  white:
    normal:     "#d8d8d8"
    bold:       "#eaeaea"

# Chrome

chrome_bgc: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['background'].lstrip('#').decode('hex')) `}"
chrome_fgc: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['foreground'].lstrip('#').decode('hex')) `}"

chrome_n_black: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['black']['normal'].lstrip('#').decode('hex')) `}"
chrome_b_black: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['black']['bold'].lstrip('#').decode('hex')) `}"
chrome_n_red: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['red']['normal'].lstrip('#').decode('hex')) `}"
chrome_b_red: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['red']['bold'].lstrip('#').decode('hex')) `}"
chrome_n_green: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['green']['normal'].lstrip('#').decode('hex')) `}"
chrome_b_green: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['green']['bold'].lstrip('#').decode('hex')) `}"
chrome_n_yellow: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['yellow']['normal'].lstrip('#').decode('hex')) `}"
chrome_b_yellow: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['yellow']['bold'].lstrip('#').decode('hex')) `}"
chrome_n_blue: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['blue']['normal'].lstrip('#').decode('hex')) `}"
chrome_b_blue: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['blue']['bold'].lstrip('#').decode('hex')) `}"
chrome_n_magenta: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['magenta']['normal'].lstrip('#').decode('hex')) `}"
chrome_b_magenta: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['magenta']['bold'].lstrip('#').decode('hex')) `}"
chrome_n_cyan: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['cyan']['normal'].lstrip('#').decode('hex')) `}"
chrome_b_cyan: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['cyan']['bold'].lstrip('#').decode('hex')) `}"
chrome_n_white: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['white']['normal'].lstrip('#').decode('hex')) `}"
chrome_b_white: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}['white']['bold'].lstrip('#').decode('hex')) `}"

chrome_n_primary: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}[{{ colors }}['primary']]['normal'].lstrip('#').decode('hex')) `}"
chrome_b_primary: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}[{{ colors }}['primary']]['bold'].lstrip('#').decode('hex')) `}"
chrome_n_secondary: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}[{{ colors }}['secondary']]['normal'].lstrip('#').decode('hex')) `}"
chrome_b_secondary: "{` '[%s, %s, %s]' % tuple(ord(c) for c in {{ colors }}[{{ colors }}['secondary']]['bold'].lstrip('#').decode('hex')) `}"

# Utility

colornumbers:
  black:    0
  red:      1
  green:    2
  yellow:   3
  blue:     4
  magenta:  5
  cyan:     6
  white:    7

irssi_colors:
  black:
    normal: '%k'
    bold:   '%K'
    bg:     '%0'
  red:
    normal: '%r'
    bold:   '%R'
    bg:     '%1'
  green:
    normal: '%g'
    bold:   '%G'
    bg:     '%2'
  yellow:
    normal: '%y'
    bold:   '%Y'
    bg:     '%3'
  blue:
    normal: '%b'
    bold:   '%B'
    bg:     '%4'
  magenta:
    normal: '%m'
    bold:   '%M'
    bg:     '%5'
  cyan:
    normal: '%c'
    bold:   '%C'
    bg:     '%6'
  white:
    normal: '%w'
    bold:   '%W'
    bg:     '%7'

# Convenience

scheme_name: "{` {{ colors }}['name'] `}"

bgc:              "{` {{ colors }}['background'] `}"
bgc_nohash:       "{` {{ colors }}['background'].lstrip('#') `}"
fgc:              "{` {{ colors }}['foreground'] `}"
fgc_nohash:       "{` {{ colors }}['foreground'].lstrip('#') `}"

n_black:          "{` {{ colors }}['black']['normal'] `}"
n_black_nohash:   "{` {{ colors }}['black']['normal'].lstrip('#') `}"
b_black:          "{` {{ colors }}['black']['bold'] `}"
b_black_nohash:   "{` {{ colors }}['black']['bold'].lstrip('#') `}"
n_red:            "{` {{ colors }}['red']['normal'] `}"
n_red_nohash:     "{` {{ colors }}['red']['normal'].lstrip('#') `}"
b_red:            "{` {{ colors }}['red']['bold'] `}"
b_red_nohash:     "{` {{ colors }}['red']['bold'].lstrip('#') `}"
n_green:          "{` {{ colors }}['green']['normal'] `}"
n_green_nohash:   "{` {{ colors }}['green']['normal'].lstrip('#') `}"
b_green:          "{` {{ colors }}['green']['bold'] `}"
b_green_nohash:   "{` {{ colors }}['green']['bold'].lstrip('#') `}"
n_yellow:         "{` {{ colors }}['yellow']['normal'] `}"
n_yellow_nohash:  "{` {{ colors }}['yellow']['normal'].lstrip('#') `}"
b_yellow:         "{` {{ colors }}['yellow']['bold'] `}"
b_yellow_nohash:  "{` {{ colors }}['yellow']['bold'].lstrip('#') `}"
n_blue:           "{` {{ colors }}['blue']['normal'] `}"
n_blue_nohash:    "{` {{ colors }}['blue']['normal'].lstrip('#') `}"
b_blue:           "{` {{ colors }}['blue']['bold'] `}"
b_blue_nohash:    "{` {{ colors }}['blue']['bold'].lstrip('#') `}"
n_magenta:        "{` {{ colors }}['magenta']['normal'] `}"
n_magenta_nohash: "{` {{ colors }}['magenta']['normal'].lstrip('#') `}"
b_magenta:        "{` {{ colors }}['magenta']['bold'] `}"
b_magenta_nohash: "{` {{ colors }}['magenta']['bold'].lstrip('#') `}"
n_cyan:           "{` {{ colors }}['cyan']['normal'] `}"
n_cyan_nohash:    "{` {{ colors }}['cyan']['normal'].lstrip('#') `}"
b_cyan:           "{` {{ colors }}['cyan']['bold'] `}"
b_cyan_nohash:    "{` {{ colors }}['cyan']['bold'].lstrip('#') `}"
n_white:          "{` {{ colors }}['white']['normal'] `}"
n_white_nohash:   "{` {{ colors }}['white']['normal'].lstrip('#') `}"
b_white:          "{` {{ colors }}['white']['bold'] `}"
b_white_nohash:   "{` {{ colors }}['white']['bold'].lstrip('#') `}"

n_primary:            "{` {{ colors }}[{{ colors }}['primary']]['normal'] `}"
n_primary_nohash:     "{` {{ colors }}[{{ colors }}['primary']]['normal'].lstrip('#') `}"
b_primary:            "{` {{ colors }}[{{ colors }}['primary']]['bold'] `}"
b_primary_nohash:     "{` {{ colors }}[{{ colors }}['primary']]['bold'].lstrip('#') `}"
n_secondary:          "{` {{ colors }}[{{ colors }}['secondary']]['normal'] `}"
n_secondary_nohash:   "{` {{ colors }}[{{ colors }}['secondary']]['normal'].lstrip('#') `}"
b_secondary:          "{` {{ colors }}[{{ colors }}['secondary']]['bold'] `}"
b_secondary_nohash:   "{` {{ colors }}[{{ colors }}['secondary']]['bold'].lstrip('#') `}"

text_primary:         "{` {{ colors }}['primary'] `}"
text_secondary:       "{` {{ colors }}['secondary'] `}"
primary_n:    "{` {{ colornumbers }}[{{ colors }}['primary']] `}"
secondary_n:  "{` {{ colornumbers }}[{{ colors }}['secondary']] `}"

ncmpcpp_primary_n:    "{` {{ colornumbers }}[{{ colors }}['primary']] + 1 `}"
ncmpcpp_secondary_n:  "{` {{ colornumbers }}[{{ colors }}['secondary']] + 1 `}"

irssi_primary_n:    "{` {{ irssi_colors }}[{{ colors }}['primary']]['normal'] `}"
irssi_primary_b:    "{` {{ irssi_colors }}[{{ colors }}['primary']]['bold'] `}"
irssi_primary_bg:   "{` {{ irssi_colors }}[{{ colors }}['primary']]['bg'] `}"
irssi_secondary_n:  "{` {{ irssi_colors }}[{{ colors }}['secondary']]['normal'] `}"
irssi_secondary_b:  "{` {{ irssi_colors }}[{{ colors }}['secondary']]['bold'] `}"
irssi_secondary_bg: "{` {{ irssi_colors }}[{{ colors }}['secondary']]['bg'] `}"

