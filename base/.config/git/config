[user]
	name = Ethan Chan
	email = metakirby5@gmail.com
[core]
	excludesfile = ~/.config/git/gitignore_global
[push]
	default = simple
[log]
	date = local
	decorate = short
[color]
	ui = auto
	diff = auto
	status = auto
	branch = auto
[alias]
	# misc
	cl = clone
	st = status --short
	co = checkout

	# commit
	ci = commit
	ca = commit --amend

	# branch
	br = branch
	ba = branch --all


	# diff
	d   = diff --color-words
	dl  = diff
	dc  = diff --color-words --cached
	dlc = diff --cached

	# add
	a  = add
	ai = add --interactive

	# stash
	s  = stash
	sl = stash list
	so = stash show
	sp = stash pop
	sa = stash apply

	# log
	l  = log --pretty=format:'%C(yellow)%h %C(green)%cd %C(blue)%an %C(reset)%s %C(magenta)%D' --date=short
	lr = log --pretty=format:'%C(yellow)%h %C(green)%cd %C(blue)%an %C(reset)%s %C(magenta)%D' --date=relative
	lp = log --pretty=format:'%C(yellow)%h %C(green)%cd %C(blue)%an %C(reset)%s %C(magenta)%D' --date=short --patch --color-words
	ls = log --pretty=format:'%C(yellow)%h %C(green)%cd %C(blue)%an %C(reset)%s %C(magenta)%D' --date=short --numstat
	lg = log --pretty=format:'%C(yellow)%h %C(green)%cd %C(blue)%an %C(reset)%s %C(magenta)%D' --date=short --graph

	# new commands
	squash = rebase interactive
	aliases = !git config -l | grep alias | cut -c 7-
