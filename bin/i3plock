#!/usr/bin/env bash

TMPBG="/tmp/i3plock.png"

# Dim until the lockscreen is up
orig_backlight="$(xbacklight -get)"
xbacklight -set 0
scrot "$TMPBG"
mogrify -scale 5% -scale 2000% "$TMPBG"
i3lock -ui "$TMPBG"
rm "$TMPBG"
xbacklight -set "$orig_backlight"

