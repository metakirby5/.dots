#!/usr/bin/env bash
# Uploads a file to teknik.io.
# Requires jq.

usage() {
  echo "USAGE: up2teknik [file]"
  exit 1
}

# Sanity checks
[ "$#" -ne 1 ] && usage
[ ! -e "$1" ] || [ -d "$1" ] && usage

curl -sfF file="@$1" https://api.teknik.io/upload/post |\
  jq .[0].results.file.url | tr -d '"'
